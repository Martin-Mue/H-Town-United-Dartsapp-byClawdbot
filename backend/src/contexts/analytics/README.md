# analytics context

This bounded context follows DDD layering:
- domain (entities, value objects, aggregates, events, repositories, services)
- application (DTOs, use-case services)
- infrastructure (controllers, repository implementations)
